/* --- 箭頭函式、ES6 變數、ES6 陣列方法--- */
let color = [1, 2, 3, 4, 5];
let result = color.filter(item => item > 2);

/* --- class 語法糖 --- */
class Circle {}

/* --- Promise 物件 --- */
const promise = Promise.resolve();
